/*
 * Page: 94, Que: 2.2 - Remove Dups
 *
 * Notes:
 *
 *
 * Mistakes:
 *
 * */

#include "..\mLinkedList.h"
/*
template<class T>
T kthtolast(mLinkedList<T> &mList, int k)
{
    mListNode<T> *current = mList.head;
    int count = 0;
    while(count < k)
    {
        current = current->next;
        count++;
    }
    mListNode<T> *mHead = mList.head;
    while(current)
    {
        current = current->next;
        mHead = mHead->next;
    }
    return mHead->val;
}*/